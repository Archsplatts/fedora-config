#!/bin/bash

set -euo pipefail

VERT='\033[0;32m'
JAUNE='\033[1;33m'
NC='\033[0m'

echo -e "${VERT}                                   ▄▄                    ${NC}"
echo -e "${VERT}                  █▄                ██       █▄  █▄      ${NC}"
echo -e "${VERT}       ▄          ██                ██      ▄██▄▄██▄     ${NC}"
echo -e "${VERT} ▄▀▀█▄ ████▄▄███▀ ████▄ ▄██▀█ ████▄ ██ ▄▀▀█▄ ██  ██ ▄██▀█${NC}"
echo -e "${VERT} ▄█▀██ ██   ██    ██ ██ ▀███▄ ██ ██ ██ ▄█▀██ ██  ██ ▀███▄${NC}"
echo -e "${VERT}▄▀█▄██▄█▀  ▄▀███▄▄██ ███▄▄██▀▄████▀▄██▄▀█▄██▄██ ▄███▄▄██▀${NC}"
echo -e "${VERT}                              ██                         ${NC}"
echo -e "${VERT}                              ▀                          ${NC}"

echo -e "${JAUNE}Bienvenue dans le script de configuration Archsplatts pour Fedora!${NC}\n"

if [ -t 0 ]; then
    read -p "Appuyez sur Entrée pour continuer... "
fi   

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[1/8] Mise à jour de Fedora${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

sudo dnf upgrade --refresh -y

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[2/8] RPM Fusion & codecs${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

sudo dnf install -y \
  https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm \
  https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm

sudo dnf swap ffmpeg-free ffmpeg --allowerasing
sudo dnf install -y mesa-va-drivers-freeworld mesa-va-drivers-freeworld.i686

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[3/8] Dépôt Terra & paquets${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

sudo dnf install --nogpgcheck --repofrompath 'terra,https://repos.fyralabs.com/terra$releasever' terra-release

TERRA_PKGS=(
	"lact" 
	"mangowm"
	"nwg-look"
	"starship"
	"ubuntu-nerd-fonts"
	"ubuntumono-nerd-fonts"
	"yazi"
)

sudo dnf install -y "${TERRA_PKGS[@]}"

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[4/8] Installation des applications${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

APPS_PKGS=(
	"discord"
	"firefox"
	"foot"
	"galculator"
	"gnome-disk-utility"
	"helvum"
	"lollypop"	
	"mediawriter"
	"mpv"
	"nm-connection-editor"
	"pavucontrol"
	"qbittorrent"
	"rhythmbox"
	"ristretto"
	"rofi"
	"sddm"
	"steam"
	"thunar"
	"thunar-archive-plugin"
	"thunar-volman"
	"thunderbird"
	"waybar"
	"wmenu"
	"zathura"
	"zathura-pdf-mupdf"
)

sudo dnf install -y "${APPS_PKGS[@]}"

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[5/8] Installation des utilitaires${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

UTILS_PKGS=(
	"bat"
	"btop"
	"calcurse"
	"cliphist"
	"du-dust"
	"eza"
	"fastfetch"
	"ffmpegthumbnailer"
	"flameshot"
	"gamemode"
	"gammastep"
	"grim"
	"java-25-openjdk"
	"kernel-modules-extra"
	"mako"
	"mangohud"
	"micro"
	"papirus-icon-theme"
	"slurp"
	"swaybg"
	"swaylock"
	"trash-cli"
	"wl-clipboard"
	"webkit2gtk4.1-devel"
	"xdg-desktop-portal-gtk" 
	"xdg-desktop-portal-wlr" 
	"xfce-polkit"
	"zsh"
	"zsh-autosuggestions"
	"zsh-syntax-highlighting"
	"zoxide"
)

sudo dnf install -y "${UTILS_PKGS[@]}"

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[6/8] Shell & interface graphique${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

chsh -s /bin/zsh 

sudo systemctl enable sddm
sudo systemctl set-default graphical.target

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[7/8] Copie des dotfiles${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

mkdir -p $HOME/.config
mkdir -p $HOME/.scripts
mkdir -p $HOME/Images/Wallpapers
mkdir -p $HOME/.local/share/themes
cd $HOME/fedora-config
cp -r config/* $HOME/.config
cp -r appearance/* $HOME/.scripts
cp zshrc $HOME/.zshrc
cp koi.jpg $HOME/Images/Wallpapers/koi.jpg
chmod u+x $HOME/.scripts/papirus-folders
chmod u+x $HOME/.config/mango/autostart.sh
chmod u+x $HOME/.config/mango/scripts/*
chmod u+x $HOME/.config/mango/waybar/scripts/*
chmod u+x $HOME/.config/rofi/scripts/*

echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${JAUNE}[8/8] Catppuccin papirus folders${NC}"
echo -e "${JAUNE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"

git clone https://github.com/catppuccin/papirus-folders.git
cd papirus-folders
sudo cp -r src/* /usr/share/icons/Papirus
$HOME/.scripts/papirus-folders -C cat-mocha-blue

echo -e "${VERT}╔════════════════════════════════════════╗${NC}"
echo -e "${VERT}║  Installation terminée avec succès !   ║${NC}"
echo -e "${VERT}╚════════════════════════════════════════╝${NC}\n"

echo -e "${JAUNE}⚠ Veuillez redémarrer votre système pour appliquer tous les changements.${NC}\n"
